import loadingGif from '../../assets/loading.gif';

export function Loading() {
  return(
    <img src={loadingGif} alt="Portal do Ricky &amp; Morty girando, indicando que o conteúdo está carregando" />
  );
}